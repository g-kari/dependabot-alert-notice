---
paths:
  - "**/*.go"
---

# Go品質チェック

- `go vet ./...` を編集後に実行
- エラーハンドリングは `fmt.Errorf("context: %w", err)` でラップ
- ログは `log/slog` を使用
- テストは `testing` 標準パッケージ + インターフェースモック
- 外部依存は最小限に（標準ライブラリ優先）
